version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: marsa_maroc_mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=marsa_maroc
    restart: always

volumes:
  mongodb_data:
